# AutoBudgetin v26.0.2 STABLE+ — GitHub Ready

Base persis dari ZIP stabil yang di-upload user (v26.0.1), ditambah fitur tracking sebagai add-on terisolasi.

Upload seluruh isi folder ini ke root repo GitHub dan replace file dengan nama yang sama. File lama yang tidak ada di paket ini boleh dibiarkan karena tidak direferensikan app shell.

Apps Script: `telegram-database-backend.gs` ditempel ke project Apps Script lalu deploy New version jika ingin notifikasi tracking baru.
